name: Pylint

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["2.7", "3.6"]
    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pylint
    - name: Analysing default.py code with pylint
      run: |
        pylint plugin.video.vstream/default.py
    - name: Analysing Hosters code with pylint
      run: |
        pylint $(find ./plugin.video.vstream/resources/hosters/ -name "*.py" | xargs)
#       Commande à mettre pour tester tous les fichiers:
#        pylint $(find . -name "*.py" | xargs)

